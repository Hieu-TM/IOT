.claude/
.cursor/
.gemini/
.kiro/
.theia/
.vscode/
build/
bin/
out/
*.local
__pycache__/
.codegraph/

# Web backend runtime data (SQLite DB + captured/mock images) — not committed
web/backend/data/

# Raw collected dataset images — too large for git, not source
dataset_collector/dataset/

# Thông tin WiFi thật của từng máy — KHÔNG commit.
# Bản mẫu để copy là firmware/aqua_scope_station/wifi_config.example.h
firmware/aqua_scope_station/wifi_config.h
dataset_collector/firmware/myconfig.h
