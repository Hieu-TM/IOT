// In nửa TRÊN khối gá — mặt tách (z=0) xuống bàn in (không support).
// Nửa trên vốn ở z 0..18; xoay để mặt tách nằm dưới, mặt có đầu-vít-chìm lên trên.
include <../constants_1d.scad>
use <../components/sensor_block_002.scad>

sensor_block_half("upper");
